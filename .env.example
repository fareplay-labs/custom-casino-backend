# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Owner wallet address (your Solana wallet that owns this backend instance)
OWNER_WALLET=YourSolanaWalletAddressHere

# Database connection
DATABASE_URL=postgresql://user:password@localhost:5432/custom_casino_db

# Redis connection
REDIS_URL=redis://localhost:6379

# JWT secret for authentication (generate a secure random string)
JWT_SECRET=your-secure-random-secret-here-change-in-production

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Backfill Settings (for catching up on blockchain history)
BACKFILL_ENABLED=true
BACKFILL_LIMIT=1000
# BACKFILL_RPC_URL=https://api.mainnet-beta.solana.com  # Optional: separate RPC for backfill

# Server Ports (defaults are fine for most setups)
# API_PORT=3000
# API_HOST=0.0.0.0
# WS_PORT=3001
# WS_HOST=0.0.0.0

# Environment
# NODE_ENV=development
# LOG_LEVEL=info
